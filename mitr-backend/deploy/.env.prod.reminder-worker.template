# Reminder worker only needs DB/Redis plus minimal runtime settings.
NODE_ENV=production
LOG_LEVEL=info
# Use sslmode=verify-full for remote Postgres.
POSTGRES_URL=
REDIS_URL=redis://redis:6379
DEVICE_TOKEN_TTL_SEC=86400
SESSION_IDLE_TIMEOUT_SEC=1800
DEVICE_CONVERSATION_IDLE_TIMEOUT_MS=20000
