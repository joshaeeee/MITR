# Digest worker only needs DB/Redis and Expo push credentials.
NODE_ENV=production
LOG_LEVEL=info
# Use sslmode=verify-full for remote Postgres.
POSTGRES_URL=
REDIS_URL=redis://redis:6379
DIGEST_JOB_CRON_UTC=* * * * *
DIGEST_DEFAULT_HOUR=20
DIGEST_DEFAULT_MINUTE=30
EXPO_ACCESS_TOKEN=
